package com.auxiliary.designer.decorator;public class Main {    public static void main(String[] args) {        SuperScore score;        score = new Score();        score = new HighDecotator(score);        score = new SortDecorator(score);        score = new BeautifulDecotator(score);        //这里的是调用链，调用的时候通过调用层次来进行调用        score.report();        score.sign("签字");    }}